# server-side/Dockerfile.worker
FROM node:18-alpine

RUN apk add --no-cache docker-cli

WORKDIR /app

# install your dependencies
COPY package*.json ./
RUN npm ci --only=production

# copy your code
COPY . .

# start the worker
CMD ["node", "src/workers/submissionWorker.js"]
